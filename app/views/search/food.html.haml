%h1 Food Results for '#{@food_query}'

%table
  %thead
    %tr
      %th{:style => "width: 20%"} Name
      %th{:style => "width: 20%"} Restaurant
      %th{:style => "width: 8%"} Rating
      %th{:style => "width: 8%"} Price
      %th{:style => "width: 24%"} Tags
      %th{:style => "width: 20%"} More Info

  %tbody
    - @foods.each do |food|
      %tr
        %td= food.name
        %td= food.restaurant.name
        %td= "#{food.rating}/5.0"
        %td= "$#{food.price}.00"
        %td= food.tags
        %td= link_to "More Info for #{food.restaurant.name}", restaurant_path(food.restaurant_id)
      %tr
%br